#!/bin/sh

# Run unit tests
npm test || exit 1

# Run E2E tests
# Git's pre-push hook provides commit information on stdin.
# WebdriverIO CLI reads stdin and treats it as a list of spec files.
# We redirect stdin from /dev/null to prevent WDIO from getting confused.
npm run e2e < /dev/null || exit 1
